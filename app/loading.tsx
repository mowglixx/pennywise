import { Spinner } from "@chakra-ui/react"


// I'm not sure why this doesn't work, not a huge issue just a pain
const Loading = () => {
    return (
        <Spinner />
    )
}
export default Loading